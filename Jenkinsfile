pipeline {
    agent any
    stages {
        stage('Example Deploy') {
            // when {
            //     branch 'production'
            // }
            steps {
                echo 'Deploying'
            }
        }
    }
}
